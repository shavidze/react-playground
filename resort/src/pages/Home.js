const Home = () => {
    return (div.a);
}

export default Home;
import React from 'react'

const Home = () => {

    return (
        <div>

        </div>
    )
};

export default Home;